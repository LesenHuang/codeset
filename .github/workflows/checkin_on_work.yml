name: CW
on:
  schedule:
  - cron: '7 9 * * 1-5'
  - cron: '17 9 * * 1-5'
jobs:
  myJob:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: job_one
      if: github.event.schedule == '7 9 * * 1-5'
      run: |
        echo job_one
        bash shell/check_in -i 15013941946 -t
    - name: job_two
      if: github.event.schedule == '17 9 * * 1-5'
      run: |
        echo job_two
        bash shell/check_in -i 15013941946 -t
